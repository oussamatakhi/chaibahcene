(()=>{'use strict';
function ensureModalControls(){document.querySelectorAll('.modal').forEach(modal=>{const card=modal.querySelector('.modal-card');if(!card)return;if(!card.querySelector('.modal-top-close')){const top=document.createElement('button');top.type='button';top.className='modal-top-close';top.setAttribute('aria-label','إغلاق');top.textContent='×';top.addEventListener('click',()=>{modal.hidden=true});card.insertBefore(top,card.firstChild)}if(!card.querySelector('.modal-bottom-close')){const bottom=document.createElement('button');bottom.type='button';bottom.className='modal-bottom-close';bottom.textContent='إغلاق';bottom.addEventListener('click',()=>{modal.hidden=true});card.appendChild(bottom)}})}
function init(){ensureModalControls();new MutationObserver(ensureModalControls).observe(document.body,{childList:true,subtree:true})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();window.ensureModalControls=ensureModalControls})();