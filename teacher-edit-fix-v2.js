(()=>{'use strict';
const S=()=>window.sb,$=id=>document.getElementById(id);
function hideLegacy(){const old=$('teacherBirthDatePlace');if(old){const l=old.closest('label');(l||old).style.display='none';}}
function ensure(){const f=$('teacherForm');if(!f)return;hideLegacy();let d=$('teacherBirthDate'),p=$('teacherBirthPlace');if(!d){const a=$('teacherAppointmentDate'),l=a?.closest('label');if(!l)return;const x=document.createElement('label');x.innerHTML='تاريخ الميلاد<input name="birth_date" id="teacherBirthDate" type="date">';l.insertAdjacentElement('beforebegin',x);d=x.querySelector('input')}if(!p&&d){const l=d.closest('label'),x=document.createElement('label');x.innerHTML='مكان الميلاد<input name="birth_place" id="teacherBirthPlace" type="text">';l.insertAdjacentElement('afterend',x);p=x.querySelector('input')}[d,p].filter(Boolean).forEach(x=>{x.disabled=false;x.readOnly=false})}
async function loadBirth(id){ensure();if(!id||!S())return;const{data,error}=await S().from('teachers').select('birth_date,birth_place').eq('id',id).maybeSingle();if(error){console.error(error);return}if(data){if($('teacherBirthDate'))$('teacherBirthDate').value=data.birth_date||'';if($('teacherBirthPlace'))$('teacherBirthPlace').value=data.birth_place||''}}
async function save(e){e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();ensure();const f=e.currentTarget,id=$('teacherId')?.value?.trim()||'',p={last_name:$('teacherLastName')?.value?.trim()||'',first_name:$('teacherFirstName')?.value?.trim()||'',rank:$('teacherRank')?.value?.trim()||null,grade:$('teacherGrade')?.value?.trim()||null,institution_id:$('teacherInstitution')?.value||null,appointment_date:$('teacherAppointmentDate')?.value||null,birth_date:$('teacherBirthDate')?.value||null,birth_place:$('teacherBirthPlace')?.value?.trim()||null,phone:$('teacherPhone')?.value?.trim()||null,email:$('teacherEmail')?.value?.trim()||null,municipality:$('teacherMunicipality')?.value?.trim()||null,notes:$('teacherNotes')?.value?.trim()||null};if(!p.last_name||!p.first_name){alert('يرجى إدخال اللقب والاسم.');return}const b=f.querySelector('button[type="submit"]');if(b){b.disabled=true;b.textContent='جارٍ الحفظ...'}try{const r=id?await S().from('teachers').update(p).eq('id',id):await S().from('teachers').insert(p);if(r.error){alert('تعذر حفظ ملف الأستاذ: '+r.error.message);return}alert('تم حفظ ملف الأستاذ بنجاح.');closeModal('teacherModal');if(typeof loadAll==='function')await loadAll()}finally{if(b){b.disabled=false;b.textContent=id?'حفظ التعديلات':'حفظ'}}}
function init(){ensure();const f=$('teacherForm');if(f&&!f.dataset.teacherSaveFix){f.dataset.teacherSaveFix='1';f.addEventListener('submit',save,true)}const m=$('teacherModal');if(m)new MutationObserver(()=>{ensure();if(!m.hidden){const id=$('teacherId')?.value?.trim();if(id&&m.dataset.birthLoadedFor!==id){m.dataset.birthLoadedFor=id;loadBirth(id)}}}).observe(m,{subtree:true,childList:true,attributes:true,attributeFilter:['hidden']})}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();})();