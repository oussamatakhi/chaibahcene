(()=>{'use strict';
function boot(){const sidebar=document.getElementById('mainSidebar'),main=document.querySelector('main.content');if(!sidebar||!main)return setTimeout(boot,200);if(document.getElementById('registrationRequests'))return;const nav=document.createElement('button');nav.className='nav';nav.dataset.section='registrationRequests';nav.textContent='طلبات تسجيل الأساتذة';const dispatchNav=sidebar.querySelector('[data-section="dispatches"]');sidebar.insertBefore(nav,dispatchNav||null);const section=document.createElement('section');section.id='registrationRequests';section.className='section';section.hidden=true;section.innerHTML='<div class="page-head"><div><h2>طلبات تسجيل الأساتذة</h2><p>مراجعة طلبات التسجيل وجدول الحصص واعتمادها.</p></div><button class="primary" id="refreshRegistrationRequests">تحديث الطلبات</button></div><div class="panel"><div id="registrationRequestsTable"><div class="empty">اختر الصفحة لعرض الطلبات.</div></div></div>';main.appendChild(section);const modal=document.createElement('div');modal.id='registrationRequestModal';modal.className='modal';modal.hidden=true;modal.innerHTML='<div class="modal-card"><button class="close" type="button" onclick="closeModal(\'registrationRequestModal\')">×</button><div id="registrationRequestDetail"></div></div>';document.body.appendChild(modal);nav.addEventListener('click',()=>{document.querySelectorAll('.nav').forEach(x=>x.classList.remove('active'));nav.classList.add('active');document.querySelectorAll('.section').forEach(x=>x.hidden=true);section.hidden=false;window.loadRegistrationRequests?.()});document.getElementById('refreshRegistrationRequests').onclick=()=>window.loadRegistrationRequests?.();}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();window.addEventListener('load',()=>setTimeout(boot,300));
})();
