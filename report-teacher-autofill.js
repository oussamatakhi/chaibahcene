(()=>{'use strict';
function formatBirthDate(v){if(!v)return '';const s=String(v).slice(0,10);const m=s.match(/^(\d{4})-(\d{2})-(\d{2})$/);return m?`${m[3]}-${m[2]}-${m[1]}`:s}
function getTeacherForVisit(visitId){const v=window.cache?.visits?.find(x=>x.id===visitId);return v?window.cache?.teachers?.find(x=>x.id===v.teacher_id):null}
function set(form,name,value){if(form?.elements?.[name])form.elements[name].value=value??''}
function applyTeacher(form,t){if(!form||!t)return;const date=formatBirthDate(t.birth_date),place=t.birth_place||'';set(form,'birth_date_place',date&&place?`${date} - ${place}`:(date||place||t.birth_date_place||''));set(form,'last_name',t.last_name||'');set(form,'first_name',t.first_name||'');set(form,'first_appointment',t.appointment_date||'');set(form,'framework',t.framework||t.rank||'');set(form,'scale',t.scale||'');set(form,'grade',t.grade||'');set(form,'grade_seniority',t.grade_seniority||'');set(form,'qualification',t.qualification||'');set(form,'nationality',t.nationality||'');set(form,'last_inspection',t.last_inspection_date||'');set(form,'previous_score',t.last_inspection_score??'');set(form,'teacher_signature_name',`${t.last_name||''} ${t.first_name||''}`.trim())}
function install(){if(typeof window.openReport!=='function')return setTimeout(install,100);if(window.__teacherReportPatched)return;window.__teacherReportPatched=true;const original=window.openReport;window.openReport=async function(visitId){const result=await original(visitId);const form=document.getElementById('ministerialReportForm');applyTeacher(form,getTeacherForVisit(visitId));return result}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install();})();