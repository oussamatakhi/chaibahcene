(()=>{'use strict';
function add(){const f=document.getElementById('teacherForm');if(!f||document.getElementById('teacherBirthDate'))return;const old=document.getElementById('teacherBirthDatePlace');if(!old)return;const wrap=old.closest('label');if(!wrap)return;wrap.style.display='none';const date=document.createElement('label');date.innerHTML='تاريخ الميلاد<input name="birth_date" id="teacherBirthDate" type="date">';const place=document.createElement('label');place.innerHTML='مكان الميلاد<input name="birth_place" id="teacherBirthPlace" type="text">';wrap.insertAdjacentElement('afterend',date);date.insertAdjacentElement('afterend',place);}
async function hydrate(){const id=document.getElementById('teacherId')?.value;if(!id)return;const sb=window.supabase?.createClient?.(window.SUPABASE_URL,window.SUPABASE_PUBLISHABLE_KEY);if(!sb)return;const {data}=await sb.from('teachers').select('birth_date,birth_place,birth_date_place').eq('id',id).maybeSingle();if(!data)return;const d=document.getElementById('teacherBirthDate'),p=document.getElementById('teacherBirthPlace'),old=document.getElementById('teacherBirthDatePlace');if(d)d.value=data.birth_date||'';if(p)p.value=data.birth_place||'';if(old&&!d?.value&&!p?.value)old.value=data.birth_date_place||'';}
function init(){add();const modal=document.getElementById('teacherModal');if(!modal)return;let lastHidden=modal.hidden;const obs=new MutationObserver(()=>{add();if(lastHidden!==modal.hidden){lastHidden=modal.hidden;if(!modal.hidden)setTimeout(hydrate,0);}});obs.observe(document.body,{subtree:true,childList:true,attributes:true,attributeFilter:['hidden']});}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();